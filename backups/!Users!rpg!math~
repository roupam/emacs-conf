;; This buffer is for notes you don't want to save, and for Lisp evaluation.
;; If you want to create a file, visit that file with C-x C-f,
;; then enter the text in that file's own buffer.

M(x) = M (x)
M(x) = M (x/2) + M (x/2+1/2)
M(x) = M (x/3) + M (x/3+1/3) + M (x/3+2/3)
M(x) = M (x/n) + M (x/n + 1/n) + ... + M (x/n + (n-1)/n)

If M_1(x) is irrational then so is gamma
If M_n(x) is irrational then at least one of M_n(x/n+m/n) is irrational


